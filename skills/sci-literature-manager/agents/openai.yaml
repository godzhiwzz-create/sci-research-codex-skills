interface:
  display_name: "SCI Literature Manager"
  short_description: "Organize and verify reusable academic literature"
  default_prompt: "Use $sci-literature-manager to handle this literature request, verify source identity, deduplicate records, and return only the needed increment or search evidence."
